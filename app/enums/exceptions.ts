export enum Exceptions {
  NOT_FOUND = 'Not found',
  INVALID_EMAIL = 'Email not valid',
  EMAIL_TEXT_REQUIRED = 'Text is required for email',
  MAILGUN_CONFIGURATION = 'Mailgun env variables are missing',
  HOST_PORT_CONFIGURATION = 'Host and/or port env variables are missing',
}
